# DS-Stack-0171

## Operation Explanation
This project implements a Stack data structure for Strings to demonstrate the `peek()` operation. The program verifies that calling `peek()` returns the top element without removing it from the stack.

## How to Run
1. Compile: `javac src/StringStack.java`
2. Run: `java src.StringStack`
